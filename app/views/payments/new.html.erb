<div class="container-recap">

  <h3 class="title">Récapitulatif de votre commande</h3>
  <div class="voucher-publish-grid text-recap text-center">
    <div><i class="fas fa-shopping-cart text-ico"></i></div>
    <div><%= @order.voucher.brand.name %>  </div>
    <div><i class="fas fa-calendar-times text-ico"></i> </div>
    <div><%= parse_date_fr(@order.voucher.end_date) %></div>
    <div><i class="fas fa-dollar-sign text-ico"></i> </div>
    <div><strong> <%= humanized_money_with_symbol @order.amount %></strong></div>
    <div><%= image_tag("logo_smartbucks.png",size: "100x40" )%></div>
    <div>Gratuit</div>
    </div>
     <div id="pay" class="button button-small-height-width position-absolute-bottom">Payer
      <script src="https://js.stripe.com/v3/"></script>
      <script>
        const paymentButton = document.getElementById('pay');
        paymentButton.addEventListener('click', () => {
          const stripe = Stripe('<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>');
          stripe.redirectToCheckout({
            sessionId: '<%= @order.checkout_session_id %>'
          });
        });
      </script>
      </div>

  </div>
</div>
