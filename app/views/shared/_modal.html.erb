<div class="modal fade" id="burger-menu" tabindex="-1" role="dialog" aria-labelledby="burger-menu" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="burger-menu">Menu</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
        <div class="modal-body">
          <ul class="navbar-nav mr-auto">
            <% if user_signed_in? %>
              <li> <%= link_to "Vendre un bon", new_voucher_path, class: "dropdown-item" %> </li>
              <li> <%= link_to "Mes annonces", profile_path, class: "dropdown-item" %></li>
              <li> <%= link_to "Aide", "#", class: "dropdown-item" %> </li>
              <li> <%= link_to "Contact", "#", class: "dropdown-item" %> </li>
              <li> <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
            <% else %>
              <li> <%= link_to "Connexion", new_user_session_path, class: "nav-link" %> </li>
            <% end %>
          </ul>
        </div>
    </div>
  </div>
</div>
